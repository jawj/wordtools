{
  "name": "anagrams",
  "version": "1.1.0",
  "description": "",
  "type": "module",
  "scripts": {
    "start": "node updateDictSizes.mjs && esbuild --bundle --format=esm --outdir=docs --loader:.svg=file --servedir=docs --watch --sourcemap=inline --define:self.DEV=true src/index.ts src/combined.worker.ts src/*.css",
    "build": "node updateDictSizes.mjs && esbuild --bundle --format=esm --outdir=docs --loader:.svg=file --minify --line-limit=100                                         src/index.ts src/combined.worker.ts src/*.css"
  },
  "author": "George MacKerron",
  "license": "ISC",
  "dependencies": {
    "comlink": "^4.4.2",
    "mithril": "^2.3.7"
  },
  "devDependencies": {
    "@types/mithril": "^2.0.6",
    "esbuild": "^0.27.2",
    "remove-accents": "^0.5.0",
    "typescript": "^5.9.3"
  }
}